#ifndef PSR_TYPES
#define PSR_TYPES

#include <stdint.h>
#include <stdlib.h>
#include <stdbool.h>

typedef uint16_t WORD;
typedef uint32_t DWORD;
typedef uint32_t LONG;
typedef uint8_t BYTE;

typedef uint8_t SECRET;

#endif